[defaults]
# Standardizes the collection path string format to resolve the warning
collections_path = ./collections

# Optional: Prevents deprecation warnings from cluttering your job logs entirely
deprecation_warnings = False
